spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catalog_db
    username: root
    password: Admin@123
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update